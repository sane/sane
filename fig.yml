{{#if database}}
db:
  image: {{database}} #postgres, mysql, mongo
  ports:
    - "{{port}}:{{port}}" #for postgres "5432:5432", for mysql "3306:3306", for mongo "27017:27017", etc
  {{#if isMysql}}
  environment:
    - MYSQL_ROOT_PASSWORD=password
    - MYSQL_DATABASE=sane
  {{/if}}
{{/if}}
server:
  image: artificial/docker-sails
  command: sails lift
  volumes:
    - server/:/server
  ports:
    - "1337:1337"
{{#if database}}
  links:
    - db
{{/if}}
  #For a future version
  #environment:
  #  - VIRTUAL_HOST=sails
  #  - VIRTUAL_PORT=1337